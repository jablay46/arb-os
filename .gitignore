# Compiler output and caches
out/
cache/
broadcast/

# Dependencies (installed via forge install / npm install)
lib/
node_modules/

# Environment / secrets
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
